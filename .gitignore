venv/
__pycache__/
*.pyc
_to_delete/
iss.tle
tle.log
gif_cache/
stats_hourly.jsonl
stats_state.json
countries.geo.json
mw.json
# Source catalogues for the object-page data. Same treatment as mw.json: the
# build scripts carry the fetch commands, the built JSON is committed, the
# multi-megabyte inputs are not. Nothing here is redistributed -- each is
# fetched from its publisher at build time.
bsc5.dat
bsc5.dat.gz
hip.tsv
gcvs.tsv
constbnd.tsv
vii1b.tsv
rngc.dat
.DS_Store
PROJECT_STATE.md
NOTES.md
DEPLOY.md

# The editor's own permission list. Local paths, local worktree names,
# and whatever tunnel was open that day -- none of it is about this project.
.claude/
